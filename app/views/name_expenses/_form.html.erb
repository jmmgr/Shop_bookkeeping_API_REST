<%= form_for(@name_expense) do |f| %>
  <% if @name_expense.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@name_expense.errors.count, "error") %> prohibited this name_expense from being saved:</h2>

      <ul>
      <% @name_expense.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
